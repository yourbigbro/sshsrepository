<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:p="http://www.springframework.org/schema/p"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
        http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-4.0.xsd
        http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.0.xsd">

	<!-- 配置controller扫描包 -->
	<context:component-scan base-package="com.itheima.controller" />

	<!-- 注解驱动(配置了这个注解驱动之后连json转换器也不用配置了) -->
	<mvc:annotation-driven conversion-service="conversionService" />
	
	<!-- 转换器配置 -->
	<bean id="conversionService" class="org.springframework.format.support.FormattingConversionServiceFactoryBean">
		<property name="converters">
			<set>
				<!-- 转换器的全路径 -->
				<bean class="com.itheima.converter.DateConverter" />
			</set>
		</property>
	</bean>

	<!-- Example: prefix="/WEB-INF/jsp/", suffix=".jsp", viewname="test" -> 
		"/WEB-INF/jsp/test.jsp" -->
	<!-- 配置视图解析器 -->
	<bean
	class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<!-- 配置逻辑视图的前缀 -->
		<property name="prefix" value="/WEB-INF/jsp/" />
		<!-- 配置逻辑视图的后缀 -->
		<property name="suffix" value=".jsp" />
	</bean>
	
	<!-- 配置全局异常处理器 (class属性值为自定义异常处理器的全路径)-->
	<bean id="customHandleException" class="com.itheima.exception.HandlExcepResol"/>

	<!-- 配置文件上传解析器。id必须设置为multipartResolver -->
	<bean id="multipartResolver"
		class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
		<!-- 设置文件上传大小 -->
		<property name="maxUploadSize" value="5000000" />
	</bean>
	
	<!-- 配置拦截器 -->
	<mvc:interceptors>
		<mvc:interceptor>
			<!-- 所有的请求都进入拦截器（注意是两个*） -->
			<!-- 先走前端控制器再走拦截器，两者都是有判断url并拦截，但两者的区别是前者拦截完了之后会去掉配置的url，而拦截器则不会去掉 -->
			<mvc:mapping path="/restfulTest/**" />
			<!-- 配置具体的拦截器 -->
			<bean class="com.itheima.interceptor.HandlerInterceptor1" />
		</mvc:interceptor>
		
	</mvc:interceptors>
	
</beans>